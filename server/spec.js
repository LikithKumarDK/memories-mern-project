/* --- *** Spec *** ---

    # Usefull Packages
    import { Low, JSONFile, LowSync, JSONFileSync } from 'lowdb';
    import { join, dirname } from 'path';
    import { fileURLToPath } from 'url';

    # Mongo Cluster
    CONNECTION_URL = mongodb+srv://LikithKumarDK:Vf6nQyBQNrVTOIaP@cluster0.sxrhdub.mongodb.net/?retryWrites=true&w=majority
    # Local
    CONNECTION_URL = mongodb://localhost:27017/latestdb
    `mongodb://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}`

*/